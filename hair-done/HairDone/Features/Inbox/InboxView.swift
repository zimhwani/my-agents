import SwiftUI

// STUB — replaced by the feature build. Keep this exact type name and initialiser.
struct InboxView: View {
    @Environment(AppState.self) private var app
    var body: some View {
        Text("InboxView").font(HDFont.heading).paperBackground()
    }
}
