import SwiftUI

// STUB — replaced by the feature build. Keep this exact type name and initialiser.
struct ProProfileEditView: View {
    @Environment(AppState.self) private var app
    var body: some View {
        Text("ProProfileEditView").font(HDFont.heading).paperBackground()
    }
}
