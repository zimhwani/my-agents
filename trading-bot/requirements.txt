# Market data for the Trading 212 path (T212's API has no price feed).
yfinance>=0.2.50
# Only needed if BROKER=ib: connection to Trader Workstation / IB Gateway.
ib_async>=1.0
# Everything else (strategy, exits, journal, Telegram, dashboard, backtest,
# the Trading 212 client) is standard library only.
# Dev:
pytest>=8
