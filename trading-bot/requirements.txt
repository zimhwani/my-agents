# Market data for the Trading 212 path (Trading 212's API has no price feed).
yfinance>=0.2.50
# Dev / tests:
pytest>=8
# Interactive Brokers users: also `pip install -r requirements-ib.txt`
# Everything else (strategy, exits, journal, Telegram, dashboard, backtest,
# the Trading 212 client) is standard library only. Python 3.11+ required.
