# Broker connection to Trader Workstation / IB Gateway (maintained fork of ib_insync).
ib_async>=1.0
# Everything else (strategy, exits, journal, Telegram, dashboard, backtest) is
# standard library only, so tests and the demo run with nothing else installed.
# Dev:
pytest>=8
